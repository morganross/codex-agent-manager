#!/bin/sh
set -eu

echo "Remote installer disabled."
echo "This repository no longer provides a shell-based bootstrap script."
echo "Use the normal Windows app and the internal daemon lifecycle instead."
exit 1
